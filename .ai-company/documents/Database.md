# Database

The current schema defines `orders` and `order_items` with trusted totals, status, timestamps, indexes, and foreign-key cleanup. The full planned model also includes products, variants, inventory, customers, addresses, payments, shipments, promotions, coupons, and audit logs.

Executable schema: `database.sql`.